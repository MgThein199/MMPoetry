<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomToolbarTitle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="fontFamily">@font/pyidaungsu</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">20sp</item> <!-- Adjust the font size as needed -->
        <item name="android:textColor">@color/colorToolbarTitle</item> <!-- Set the desired text color -->
        <!-- You can add more attributes here, like padding, gravity, etc. -->
    </style>
    <style name="JsonTest_Sepia" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowDisablePreview">true</item>

        <item name="android:statusBarColor">@color/colorStatusBar</item>
        <item name="android:windowBackground">@color/colorWindowBackground</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/colorText</item>
        <item name="backgroundColor">@color/colorLayoutBackground</item>
    </style>
</resources>